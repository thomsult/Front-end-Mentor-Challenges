import DestinationSection from "../Component/Section/destination";

import Layout from "../Component/Background/layout";

function destination() {
  return (
    <Layout Destination={"destination"}>
      <DestinationSection />
    </Layout>
  );
}

export default destination;
